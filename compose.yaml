services:
  postgres:
    image: postgres:18-alpine
    container_name: domus-pg
    env_file: ".env"
    ports:
      - "5432:5432"
    volumes:
      - domus-pgdata:/var/lib/postgresql/data

volumes:
  domus-pgdata:
